var div_id = $('#content');
var content = "<h4>Followers</h4>";

<% if @user.followers_count_by_type("user") != 0 %>
	<% @user.followers_by_type("user").each do |follow| %>
		content += "<%=follow.name%>" + "<br>";
	<% end%>
<% end%>

div_id.html(content);