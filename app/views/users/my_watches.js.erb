var div_id = $('#content');
var content = "<h4>Watching</h4>";

<% if current_user.followees_count_by_type("event") != 0 %>
	<% current_user.followees_by_type("event").each do |watch| %>
		content += "<%=watch.title%>" + "<br>";
	<% end%>
<% end%>

div_id.html(content);